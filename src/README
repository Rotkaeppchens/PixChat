The src root for the client side.

main.cpp        - The main function for the system
client_main.h   - The client main header for the standard main
client_main.cpp - The client main source for the standard main

To build use the make file with "make build"
To build with the documentation use "make build_extra"

To install copy the env/client folder to the place you want them.
